services:
  postgres:
    image: postgres:17-alpine
    container_name: secrets-api-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: secrets
    ports:
      - "5432:5432"
